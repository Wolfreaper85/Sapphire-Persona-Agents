// Persona Agents — index.js (settings tab stub)
// The main view lives in main.js (auto-loaded, registers nav + view).
// This file exists so the plugin system detects web: true for settings.

export default {
    init() {
        // No settings tab needed — the full UI is in the standalone view
    }
};
